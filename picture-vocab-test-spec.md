# 그림어휘력 검사 개발 사양서

명사·동사·형용사 평가용 수용어휘력 검사
부산가톨릭대학교 언어청각치료학과 | 문서 버전 0.2

---

## 0. v0.1에서 변경된 사항

| 항목 | v0.1 | v0.2 |
|---|---|---|
| 대상 연령 | 미정 | 만 2;6 ~ 16;11 |
| 문항 수 | 20 (프로토타입) | 150 (개발) → 110~120 (확정 예정) |
| 품사 | 명사, 동사 | 명사, 동사, 형용사 |
| 개발 방식 | 미정 | 1·2단계 동시 개발, 성인 버전은 별도 과제로 분리 |
| 문항 식별 | 문항번호 = 제시순서 | 문항ID(영구 고정)와 순번(가변)을 분리 |
| 기저점·최고한계점 | 없음 | 도입 (§8) |

문항 목록은 이 문서에 싣지 않는다. `items.json`과 `그림어휘력검사_문항풀_150.xlsx`가 원본이며, 두 파일의 내용은 동일하다.

## 1. 문서의 목적

이 문서는 명사·동사·형용사를 평가하는 수용어휘력 검사 프로그램을 개발하기 위한 기술 사양서다.

개발 도구가 이 문서와 `items.json`만으로 문항 데이터 구조, 그림 자산 규격, 실시 로직, 채점 로직을 일관되게 구현할 수 있도록 작성되었다. 이 문서에 명시되지 않은 구조를 임의로 정하지 말고, 불명확한 부분은 구현 전에 확인할 것.

## 2. 검사 개요

- 검사명(가칭): 명사·동사·형용사 그림어휘력 검사
- 측정 대상: 수용어휘력 (표현어휘력은 이 검사의 범위가 아니다)
- 대상 연령: 만 2세 6개월 ~ 만 16세 11개월
- 반응 형식: 4지선다. 화면에 4개의 그림을 제시하고 검사자가 목표어휘를 구어로 들려주면 피검자가 해당 그림을 선택
- 개발 문항 수: 150문항
- 확정 예정 문항 수: 110~120문항 (예비조사 및 문항분석에서 통상 20~30% 탈락)
- 총 그림 자산 수: 600장 (150문항 × 4). 확정 후 440~480장
- 채점: 정반응 1점, 오반응 0점
- 산출: 총점, 품사별 하위점수, 오답 유형별 빈도, 문항별 반응시간

### 2.1 참조 검사

| 검사 | 대상 연령 | 문항 수 | 품사 구성 |
|---|---|---|---|
| REVT-R (김영태 외, 2009) | 2;6 ~ 16세 이상 성인 | 185 | 명사 98(53%), 동사 68(37%), 형용사·부사 19(10%) |
| 그림어휘력검사 PPVT-K (1995) | 2;0 ~ 8;11 | 112 | — |
| PPVT-4 (Pearson, 2007) | 2;6 ~ 90세 이상 | 228 | 명사, 동사, 속성어(형용사+부사) |

REVT-R의 연간 문항 밀도는 약 13.5문항/년이다. 본 검사의 문항 수는 이 값을 기준으로 산정했다.

## 3. 개발 단계 구조

최종적으로 **하나의 검사**이며, 문항을 연령 구간별로 나눠 순차 개발하는 것이 아니라 1·2단계를 동시에 개발한다. 성인 버전만 별도 과제로 분리한다.

| 단계 | 연령 구간 | 문항 수 | 밀도 | 상태 |
|---|---|---|---|---|
| 1단계 | 2;6 ~ 8;11 | 92 | 14.2문항/년 | 개발 중 |
| 2단계 | 9;0 ~ 16;11 | 58 | 7.3문항/년 | 개발 중 |
| 성인 버전 | 17세 이상 | — | — | 별도 과제 |

문항을 균등 배분하지 않고 저연령에 가중했다. 임상적 진단과 치료목표 설정이 학령전기·초등 저학년에 집중되므로 해당 구간의 측정 정밀도를 우선한 결과다.

### 3.1 난이도 구간

문항은 예상 습득 연령에 따라 6개 구간으로 잠정 배정되어 있다. **이 배정은 추정치이며 경험적 근거가 아니다.** 예비조사 후 IRT로 재추정하면 상당수 문항이 다른 구간으로 이동한다.

| 구간 | 예상 연령 | 문항 수 | 순번 범위 | 단계 |
|---|---|---|---|---|
| A | 2;6 ~ 3;11 | 28 | 1 ~ 28 | 1 |
| B | 4;0 ~ 5;11 | 34 | 29 ~ 62 | 1 |
| C | 6;0 ~ 8;11 | 30 | 63 ~ 92 | 1 |
| D | 9;0 ~ 11;11 | 26 | 93 ~ 118 | 2 |
| E | 12;0 ~ 14;11 | 18 | 119 ~ 136 | 2 |
| F | 15;0 ~ 16;11 | 14 | 137 ~ 150 | 2 |

### 3.2 성인 버전 분리에 따른 제약

성인 버전을 별도로 개발하면 **청소년과 성인의 점수를 직접 비교할 수 없다.** 향후 두 검사를 연계하려면 공통문항(anchor items)을 통한 수직척도화가 필요하다. 이를 위해 F구간(15;0~16;11) 문항을 여유 있게 개발해 두었으며, 성인 버전 개발 시 이 문항들을 성인 표본에도 실시하면 연계가 가능하다.

## 4. 품사 구성

| 품사 | 문항 수 | 비율 | REVT-R |
|---|---|---|---|
| 명사 | 78 | 52% | 53% |
| 동사 | 52 | 35% | 37% |
| 형용사 | 20 | 13% | 10% (형용사+부사) |

REVT-R의 비율을 따르되 형용사를 소폭 상향했다. 10%(19개)는 하위점수의 신뢰도를 확보하기에 부족하고, REVT의 수치는 부사를 포함한 값이기 때문이다.

**이 비율은 모든 난이도 구간에서 동일하게 유지한다.** 구간마다 비율이 달라지면 8세 아동의 동사 점수와 12세 아동의 동사 점수가 서로 다른 구성의 문항에서 나오게 되어, 품사별 하위점수를 척도 전체에서 비교할 수 없다. 문항을 추가·삭제·교체할 때 이 원칙을 반드시 확인할 것.

### 4.1 형용사의 제약

형용사 문항은 **그림으로 제시 가능한 구체적 속성어로만** 구성했다. '정직하다, 관대하다, 신중하다' 같은 추상 형용사는 4지선다 그림 형식으로 측정할 수 없다.

이 때문에 형용사는 명사보다 난이도 상한이 낮고, F구간에서도 '옴폭하다, 자욱하다, 헐겁다' 수준에 머문다. 청소년기 형용사 어휘를 본격적으로 측정하려면 정의 과제나 문맥 과제 등 다른 형식이 필요하며, 이는 본 검사의 범위 밖이다.

## 5. 오답(방해자극) 설계 원칙

각 문항의 오답 3개는 서로 다른 기능을 담당한다. 이 구조 덕분에 총점뿐 아니라 오답 유형별 반응 패턴을 통한 오류 분석이 가능하다. 프로그램은 반드시 각 선택지에 역할 코드를 함께 저장해야 한다.

| 역할 코드 | 유형 | 기능 및 오류 해석 |
|---|---|---|
| `tgt` | 정답 | — |
| `sem` | 의미적 방해자극 | 목표어와 동일한 의미범주에 속하는 어휘. 선택 시 범주 지식은 형성되었으나 세부 어휘 지식이 부족한 상태로 해석한다. |
| `vis` | 시각적 방해자극 | 그림상 형태·색·윤곽이 유사한 어휘. 선택 시 어휘 지식 없이 시지각적 유사성에 근거해 추측한 것으로 해석한다. |
| `unr` | 무관 방해자극 | 의미·시각 모두 무관한 어휘. 선택 시 무선 반응(random guessing) 또는 과제 이해 실패를 시사한다. |

### 5.1 예외 문항

`N026`(팔꿈치)은 오답 3개가 모두 신체부위이며 역할 코드는 `sem1`/`sem2`/`sem3`이다. 신체부위 문항에 무관 자극을 넣으면 난이도가 급격히 낮아져 관절 명칭 변별력을 측정할 수 없기 때문이다.

프로그램은 역할 코드를 `sem`/`vis`/`unr` 세 값으로 하드코딩하지 말고, `items.json`의 값을 그대로 읽어 처리해야 한다. 오답 유형별 집계에서 `sem1`~`sem3`은 모두 `sem`으로 합산한다.

## 6. 문항 데이터 구조

문항 데이터는 코드에 하드코딩하지 말고 `items.json`을 읽는다.

```json
{
  "test_name": "명사·동사·형용사 그림어휘력 검사",
  "version": "0.2-item-pool",
  "target_age": "2;6 - 16;11",
  "item_count": 150,
  "composition": { "noun": 78, "verb": 52, "adjective": 20 },
  "items": [
    {
      "item_id": "N001",
      "sequence": 1,
      "phase": 1,
      "band": "A",
      "expected_age": "2;6-3;11",
      "word_class": "noun",
      "word_class_ko": "명사",
      "target": "공",
      "answer_position": 3,
      "difficulty": null,
      "options": [
        { "position": 1, "word": "풍선", "role": "sem", "image": "item_N001_sem.png" },
        { "position": 2, "word": "사과", "role": "vis", "image": "item_N001_vis.png" },
        { "position": 3, "word": "공",   "role": "tgt", "image": "item_N001_tgt.png" },
        { "position": 4, "word": "양말", "role": "unr", "image": "item_N001_unr.png" }
      ],
      "note": ""
    }
  ]
}
```

### 6.1 문항 ID와 순번의 분리

이것은 프로그램 설계에 직접 영향을 준다.

- **`item_id`**: 영구 고정. 품사 접두어(N=명사, V=동사, A=형용사) + 3자리 일련번호. 한번 부여하면 절대 변경하지 않으며, 폐기된 문항의 번호도 재사용하지 않는다. 그림 파일명과 수집 데이터가 모두 이 값을 참조한다.
- **`sequence`**: 제시 순서. 예비조사로 난이도가 재추정되면 변경된다.

두 값을 하나로 합치면, 난이도 재배열 시 전체 번호를 다시 매겨야 하고 이전 조사 데이터와 대조가 불가능해진다.

### 6.2 정합성 검증

프로그램은 기동 시 아래를 검증하고 위반 시 오류를 발생시킨다.

- `answer_position`이 `options` 중 `role`이 `tgt`인 항목의 `position`과 일치하는가
- `options`의 `position`이 1~4로 중복 없이 채워져 있는가
- 참조된 그림 파일이 모두 존재하는가
- `item_id`가 중복되지 않는가

## 7. 그림 자산

### 7.1 파일 네이밍

```
item_{item_id}_{역할코드}.png

예시
  item_N001_tgt.png   (공)
  item_N001_sem.png   (풍선)
  item_N001_vis.png   (사과)
  item_N001_unr.png   (양말)

예외 문항 (N026 팔꿈치)
  item_N026_tgt.png   (팔꿈치)
  item_N026_sem1.png  (무릎)
  item_N026_sem2.png  (어깨)
  item_N026_sem3.png  (발목)
```

모든 그림은 `assets/images/`에 저장한다. 파일명이 `item_id`를 쓰므로 순번이 바뀌어도 파일을 건드릴 필요가 없다.

### 7.2 공통 규격

- 캔버스: 정사각형 1024 × 1024 px, PNG, 흰색 배경(투명 배경 아님)
- 사물 점유 면적: 캔버스 대비 55~75% 범위로 통일. 특정 그림만 크게 그려지면 크기 자체가 단서가 된다
- 여백: 사방 최소 8% 이상 확보
- 화풍: 600장 전체가 동일한 선 굵기, 동일한 채색 방식, 동일한 명암 처리를 사용
- 색채 통제: 색이 정답 단서가 되지 않도록 채도와 명도를 맞춘다. 사과–토마토, 옥수수–바나나처럼 색이 주요 변별 요소인 문항은 반드시 검토
- 문자·숫자·로고 완전 배제
- 문화적 편향 배제: 국내 아동에게 친숙한 형태로 그린다

### 7.3 동사 문항 추가 규격

- 한 문항의 4개 그림에서 행위자의 성별·연령·복장·머리 모양·배경·대상물이 완전히 동일해야 하며, 오직 동작만 달라져야 한다
- 배경이 다르면 아동이 어휘가 아닌 장면 단서로 답을 고르게 된다
- 상태변화 동사(젖다, 응결하다 등)는 정적인 결과 장면으로 그리되 원인(비, 물웅덩이 등)을 포함하지 않는다. 원인 단서가 들어가면 어휘 지식이 아닌 추론을 측정하게 된다
- 동사 그림은 명명 일치도가 명사보다 낮게 나오므로 대안 시안을 2~3개씩 준비한다

### 7.4 형용사 문항 추가 규격

- 4개 그림에서 **속성을 제외한 모든 요소가 동일한 사물**이어야 한다. 예: '무겁다' 문항은 동일한 상자 네 개가 무게만 다르게 표현되어야 한다
- 속성이 정도(degree)로 표현되는 경우, 인접 선택지와의 차이가 명확히 지각 가능해야 한다
- 반의어를 오답으로 쓸 때 두 그림의 대비가 과도하면 난이도가 급락한다

### 7.5 제작 경로와 라이선스

프로토타입 단계에서는 AAC 심볼 라이브러리와 AI 이미지 생성을 혼합해 사용한다. 정식 검사로 확장할 때는 저작권 전부양도 계약을 맺은 일러스트레이터 외주로 전환한다.

> **라이선스 주의**: ARASAAC 픽토그램은 CC BY-NC-SA로 비영리 사용만 허용되며 상업적 제품·출판물 포함이 명시적으로 제외된다. Mulberry Symbols는 CC BY-SA로 상업적 이용은 가능하지만 ShareAlike 조건 때문에 파생물인 검사 도구 전체를 동일 라이선스로 공개해야 한다. 두 라이브러리 모두 유료 검사 도구에는 사용할 수 없다.

### 7.6 제작 시점

**전문가 내용타당도 검증을 통과한 문항에 대해서만 그림을 제작한다.** 150문항 전체의 그림을 먼저 만들면 탈락 문항의 제작 비용이 그대로 매몰된다. 150문항 기준 600장과 120문항 기준 480장의 차이는 그림 120장분이다.

## 8. 실시 규칙

### 8.1 기저점·최고한계점 (잠정)

전 문항을 실시하지 않고 피검자의 능력 수준 부근 문항만 실시한다.

- **시작 문항**: 생활연령에 따라 §8.2 표를 적용
- **기저점**: 연속 8문항 정답. 미달 시 시작 문항 이전으로 역순 실시
- **최고한계점**: 연속 8문항 중 6문항 오류
- **원점수**: 최고한계점 문항 번호 − 총 오류 수

이 규칙은 잠정값이다. 예비조사 후 실제 반응 분포를 보고 확정한다. 규칙을 설정 파일로 분리해 코드 수정 없이 변경할 수 있게 구현할 것.

### 8.2 연령별 시작 문항 (잠정)

| 생활연령 | 시작 순번 | 생활연령 | 시작 순번 |
|---|---|---|---|
| 2;6 ~ 2;11 | 1 | 9;0 ~ 9;11 | 93 |
| 3;0 ~ 3;11 | 8 | 10;0 ~ 10;11 | 100 |
| 4;0 ~ 4;11 | 29 | 11;0 ~ 11;11 | 108 |
| 5;0 ~ 5;11 | 40 | 12;0 ~ 12;11 | 119 |
| 6;0 ~ 6;11 | 63 | 13;0 ~ 13;11 | 125 |
| 7;0 ~ 7;11 | 72 | 14;0 ~ 14;11 | 131 |
| 8;0 ~ 8;11 | 82 | 15;0 ~ 16;11 | 137 |

발달지체가 의심되는 경우 생활연령보다 낮은 시작 문항을 적용할 수 있으며, 기저점 규칙이 이를 자동으로 보정한다.

### 8.3 그 밖의 실시 규칙

- 그림은 2×2 격자로 배치하고 위치 번호는 1=좌상, 2=우상, 3=좌하, 4=우하
- 정답 위치는 1~4번에 균등 배분되어 있다(38/37/37/38). 프로그램이 임의로 섞지 않는다
- 시간 제한은 두지 않는다. 단 문항별 반응시간(ms)은 기록한다
- 무반응 처리: 검사자가 '무반응' 버튼을 누를 수 있어야 하며, 이 경우 0점이되 오답 유형 집계에서는 제외한다
- 연습 문항 3개를 본 문항 앞에 배치한다. 연습 문항은 채점에 포함하지 않으며 `items.json`과 별도로 관리한다

## 9. 기록 및 결과 출력

### 9.1 문항별 기록

`item_id`, 제시 순번, 선택 위치, 선택 어휘, 선택지 역할 코드, 정오, 반응시간(ms), 무반응 여부

### 9.2 결과 요약

- 총점(원점수)
- 품사별 하위점수: 명사, 동사, 형용사 각각의 실시 문항 수 대비 정답 수
- 오답 유형별 빈도: `sem`(`sem1`~`sem3` 합산), `vis`, `unr`
- 평균 반응시간, 품사별 평균 반응시간
- 기저점 문항, 최고한계점 문항

규준이 확보되기 전까지는 표준점수·백분위·어휘발달연령을 산출하지 않는다. 프로그램은 이 항목들의 자리를 마련하되 '규준 미확보'로 표시한다.

### 9.3 내보내기

- CSV 파일명: `result_{피검자ID}_{YYYYMMDD_HHMMSS}.csv`
- 문항별 기록과 결과 요약을 각각 별도 CSV로 출력
- 개인정보는 피검자ID, 생년월(연-월까지만), 성별만 수집한다. 이름과 생년월일 전체는 저장하지 않는다

## 10. 검증 절차

순서를 지킬 것. 뒤 단계를 먼저 하면 앞 단계의 결과로 폐기될 작업에 비용이 들어간다.

### 10.1 전문가 내용타당도 검증 (그림 제작 이전)

- 언어재활사 및 관련 전공 교수를 대상으로 문항 적절성, 오답 배치의 타당성, 연령 적합성을 평정
- CVI(내용타당도 지수) 산출, 기준 미달 문항 수정 또는 폐기
- 이 단계에서 150문항이 130문항 내외로 줄어드는 것이 정상이다

### 10.2 그림 명명 일치도 검증

- 정상 성인 20~30명에게 각 그림을 단독으로 제시하고 자유롭게 이름을 말하게 한다
- 목표 어휘로 명명한 비율이 80~90% 이상인지 확인
- 기준 미달 그림은 수정 또는 교체. 통과하지 못한 그림은 어휘 지식이 아니라 그림 자체의 모호성을 측정하게 된다
- 동사와 형용사는 명사보다 일치도가 낮게 나오므로 대안 시안으로 재검증

### 10.3 예비조사 및 문항분석

- 각 연령대별 표본을 대상으로 실시
- IRT로 문항 난이도(b)와 변별도(a) 추정, `items.json`의 `difficulty` 필드에 기입
- 난이도 순으로 `sequence` 재배열 (`item_id`는 유지)
- 변별도 미달 문항, 난이도가 지나치게 편중된 문항 폐기
- 최종 110~120문항 확정

### 10.4 표준화

- 연령대별 표본 확보 후 규준 산출
- 신뢰도: 재검사 신뢰도, 반분 신뢰도
- 타당도: REVT 수용어휘력검사와의 공인타당도

## 11. 미결정 사항

구현 시 이 항목들에 의존하는 로직은 설정값으로 분리해 두고, 임의로 값을 정하지 말 것.

- 실시 플랫폼(웹 / 태블릿 앱 / 데스크톱)과 검사자 조작 방식
- 상업적 출판 여부. 확정 후 그림 자산의 저작권 확보 방식이 결정된다
- 기저점·최고한계점 규칙의 최종값 (§8.1은 잠정)
- 연령별 시작 문항표의 최종값 (§8.2는 잠정)
- 연습 문항 3개의 내용
- 표준화 표본 규모 및 지역 배분
- 성인 버전 착수 시점 및 공통문항 설계
